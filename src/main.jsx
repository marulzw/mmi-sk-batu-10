import React from "react";
import { createRoot } from "react-dom/client";
import BorangMMIApp from "./App.jsx";

createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <BorangMMIApp />
  </React.StrictMode>
);
